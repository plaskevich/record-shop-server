import { ShopModel } from '../../models/Shop';

export async function checkUserShop(userId: string, shopId: string) {
    // const shop = await ShopModel.findById(shopId)
    // if (shop?.users.some(item => item.userId === userId)) {
    //     return true
    // } else return false
}
export async function getUserShop(userId: string) {
    // const shop = await ShopModel.find
    // if (shop?.users.some(item => item.userId === userId)) {
    //     return true
    // } else return false
}